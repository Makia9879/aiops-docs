services:
  nginx:
    image: nginx:1.27-alpine
    container_name: aiops-docs-nginx
    ports:
      - "8080:80"
    volumes:
      - ./docs/.vuepress/dist:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
